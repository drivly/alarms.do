{
  "name": "alarms.do",
  "module": "worker.js",
  "version": "1.0.0",
  "description": "A Durable Object for managing cloud-driven alarms to URL callbacks",
  "repository": "git@github.com:drivly/alarms.do.git",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev worker.js --local true",
    "deploy": "wrangler publish worker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "devDependencies": {
    "eslint": "^8.23.0",
    "jest": "^28.1.3",
    "prettier": "^2.7.1",
    "wrangler": "^2.0.27"
  }
}
